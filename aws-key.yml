---
- hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: creating the key
      ec2_key:
        name: sample
        region: us-east-1
      register: keyout

    - name: storing the key
      copy:
        content: "{{keyout.key.private_key}}"
        dest: ./sample-key.pem

      