apiVersion: apps/v1
kind: Deployment
metadata:
  name: vproapp
spec:
  selector:
    matchLabels:
      app: vproapp
  template:
    metadata:
      labels:
        app: vproapp
    spec:
      containers:
        - name: vproapp
          image: udaymac/vproapp:latest
          ports:
          - containerPort: 8080
            name: vproapp-port
      initContainers:
        - name: init-db
          image: busybox
          command: ['sh', '-c', "until nslookup vprodb; do echo waiting for mydb-service; sleep 2; done"]
        
        - name: init-mc
          image: busybox
          command: ['sh', '-c', "until nslookup vpromc; sleep 2; done"]